# Java后端实习面试精华题目（系统设计篇）

## 七、系统设计

### 7.1 系统设计基础

#### 7.1.1 设计原则
**问题：** 系统设计的基本原则有哪些？

**参考答案：**
1. **可扩展性（Scalability）**：系统能够处理增长的工作负载
2. **可用性（Availability）**：系统能够持续正常运行
3. **可靠性（Reliability）**：系统能够正确处理请求，不丢失数据
4. **可维护性（Maintainability）**：系统易于修改和维护
5. **性能（Performance）**：系统响应时间合理，吞吐量高
6. **安全性（Security）**：系统能够保护数据和资源

**问题：** CAP理论是什么？在实际系统中如何权衡？

**参考答案：**
**CAP理论：** 分布式系统无法同时满足以下三个特性：
- **一致性（Consistency）**：所有节点在同一时间看到相同的数据
- **可用性（Availability）**：每个请求都能收到响应
- **分区容错性（Partition tolerance）**：系统在节点间通信失败时仍能工作

**实际权衡：**
- **CP系统**：牺牲可用性，保证一致性和分区容错性（如分布式数据库）
- **AP系统**：牺牲一致性，保证可用性和分区容错性（如缓存系统）
- **CA系统**：单机系统，不适用于分布式环境

#### 7.1.2 架构模式
**问题：** 常见的系统架构模式有哪些？

**参考答案：**
1. **单体架构（Monolithic）**：所有功能在一个应用中
   - 优点：简单、易于部署
   - 缺点：扩展困难、维护成本高

2. **分层架构（Layered）**：按功能分层（表现层、业务层、数据层）
   - 优点：职责清晰、易于测试
   - 缺点：层间耦合、性能开销

3. **微服务架构（Microservices）**：按业务能力拆分服务
   - 优点：独立部署、技术异构、弹性扩展
   - 缺点：运维复杂、数据一致性难保证

4. **事件驱动架构（Event-driven）**：基于事件进行通信
   - 优点：松耦合、异步处理、实时性好
   - 缺点：调试困难、消息顺序问题

### 7.2 高可用设计

#### 7.2.1 冗余设计
**问题：** 如何实现系统的高可用？

**参考答案：**
1. **硬件冗余：**
   - 服务器集群
   - 负载均衡器
   - 冗余网络设备

2. **软件冗余：**
   - 服务多实例部署
   - 数据库主从复制
   - 分布式缓存

3. **数据冗余：**
   - 数据备份
   - 异地多活
   - 数据同步

**问题：** 什么是异地多活架构？

**参考答案：**
**异地多活：** 在不同地理位置部署完整的应用系统，都能独立提供服务

**实现要点：**
1. **数据同步：** 使用消息队列、数据库复制等技术
2. **流量调度：** 基于地理位置的DNS解析
3. **冲突解决：** 数据合并策略、版本控制
4. **监控切换：** 自动故障检测和切换

#### 7.2.2 故障处理
**问题：** 常见的容错机制有哪些？

**参考答案：**
1. **超时机制：** 设置合理的超时时间
2. **重试机制：** 指数退避重试
3. **熔断机制：** 服务故障时快速失败
4. **降级机制：** 核心功能优先，非核心功能降级
5. **限流机制：** 防止系统过载

**问题：** 如何实现服务的熔断和降级？

**参考答案：**
```java
// 使用Hystrix实现熔断
@HystrixCommand(
    fallbackMethod = "fallback",
    commandProperties = {
        @HystrixProperty(name = "execution.isolation.thread.timeoutInMilliseconds", value = "1000"),
        @HystrixProperty(name = "circuitBreaker.errorThresholdPercentage", value = "50")
    }
)
public String callService() {
    // 业务逻辑
}

public String fallback() {
    return "服务暂时不可用";
}

// 降级策略
public Data getData() {
    try {
        return primaryService.getData();
    } catch (Exception e) {
        return cacheService.getData();
    }
}
```

### 7.3 高性能设计

#### 7.3.1 缓存策略
**问题：** 缓存的使用场景和设计考虑？

**参考答案：**
**使用场景：**
1. **读多写少：** 用户信息、商品信息
2. **计算成本高：** 复杂查询结果、聚合数据
3. **第三方服务：** API调用结果缓存

**设计考虑：**
1. **缓存粒度：** 选择合适的缓存单位
2. **缓存策略：** LRU、LFU、TTL等
3. **缓存更新：** 主动更新、被动更新
4. **缓存穿透：** 空值缓存、布隆过滤器
5. **缓存雪崩：** 随机过期时间、多级缓存

**问题：** 如何解决缓存一致性问题？

**参考答案：**
1. **Cache-Aside模式：**
   ```
   读：缓存未命中 → 读数据库 → 写缓存
   写：写数据库 → 删除缓存
   ```

2. **Read-Through模式：** 缓存层自动处理数据库读写
3. **Write-Through模式：** 写操作同时更新缓存和数据库
4. **Write-Behind模式：** 异步批量更新数据库

#### 7.3.2 数据库优化
**问题：** 数据库性能优化的策略？

**参考答案：**
1. **SQL优化：**
   - 合理使用索引
   - 避免SELECT *
   - 减少子查询
   - 优化JOIN操作

2. **架构优化：**
   - 读写分离
   - 分库分表
   - 使用缓存
   - 异步处理

3. **配置优化：**
   - 连接池配置
   - 存储引擎选择

**问题：** 分库分表的策略和挑战？

**参考答案：**
**分库策略：**
- **垂直分库：** 按业务拆分
- **水平分库：** 按数据特征拆分

**分表策略：**
- **范围分表：** 按时间、ID范围
- **哈希分表：** 按哈希值
- **地理分表：** 按地理位置

**挑战：**
- 跨分片查询
- 分布式事务
- 全局ID生成
- 数据迁移

### 7.4 高并发设计

#### 7.4.1 并发控制
**问题：** 如何处理高并发场景？

**参考答案：**
1. **前端优化：**
   - CDN加速
   - 浏览器缓存
   - 资源压缩

2. **应用层优化：**
   - 异步处理
   - 消息队列
   - 连接池

3. **数据层优化：**
   - 缓存
   - 数据库连接池
   - 读写分离

4. **架构优化：**
   - 负载均衡
   - 服务拆分
   - 弹性扩展

**问题：** 什么是秒杀系统？如何设计？

**参考答案：**
**秒杀系统特点：**
- 瞬时高并发
- 库存有限
- 防止超卖

**设计要点：**
1. **前端：**
   - 页面静态化
   - 验证码
   - 按钮置灰

2. **应用层：**
   - 限流
   - 队列削峰
   - 缓存预热

3. **数据层：**
   - Redis原子操作扣减库存
   - 异步写入数据库
   - 库存分段

#### 7.4.2 消息队列
**问题：** 消息队列的使用场景？

**参考答案：**
1. **异步处理：** 发送邮件、短信
2. **应用解耦：** 订单系统 → 库存系统
3. **流量削峰：** 秒杀、抢购
4. **日志收集：** 系统日志、用户行为
5. **事件驱动：** 状态变更通知

**问题：** 如何保证消息的可靠性？

**参考答案：**
1. **生产者：**
   - 消息确认机制
   - 失败重试
   - 本地消息表

2. **消息队列：**
   - 持久化
   - 主从复制
   - 集群部署

3. **消费者：**
   - 手动确认
   - 幂等处理
   - 死信队列

### 7.5 具体系统设计

#### 7.5.1 短网址系统
**问题：** 设计一个短网址系统（如bit.ly）

**参考答案：**
**需求分析：**
- 将长URL转换为短URL
- 短URL重定向到原始URL
- 高并发访问
- 数据统计

**系统设计：**
1. **URL映射：**
   ```
   长URL → 哈希算法 → 短码 → 存储映射关系
   ```

2. **哈希算法：**
   - Base62编码（0-9, a-z, A-Z）
   - 6位短码：62^6 ≈ 568亿组合

3. **存储设计：**
   ```
   | id | short_code | original_url | created_at | access_count |
   ```

4. **缓存策略：**
   - Redis缓存热点URL映射
   - LRU淘汰策略

5. **性能优化：**
   - CDN缓存短URL页面
   - 数据库读写分离
   - 异步统计访问数据

**问题：** 如何处理哈希冲突？

**参考答案：**
1. **重试机制：** 生成新短码直到不冲突
2. **加盐哈希：** 在原始URL后添加随机字符串
3. **递增ID：** 使用自增ID转换为Base62

#### 7.5.2 微博/朋友圈系统
**问题：** 设计一个微博或朋友圈系统

**参考答案：**
**需求分析：**
- 发布动态
- 关注/取关
- 查看动态流
- 点赞、评论

**系统设计：**
1. **数据模型：**
   ```
   User: id, name, followers, following
   Post: id, user_id, content, timestamp, likes
   Follow: follower_id, followee_id
   ```

2. **动态流设计：**
   **推模式（写扩散）：**
   - 用户发动态时，推送给所有粉丝
   - 优点：读取快
   - 缺点：大V发动态成本高

   **拉模式（读扩散）：**
   - 用户读取时，拉取关注的人的最新动态
   - 优点：发动态成本低
   - 缺点：读取时需要聚合

   **混合模式：**
   - 普通用户用推模式
   - 大V用拉模式

3. **存储设计：**
   - 用户关系：图数据库或关系数据库
   - 动态内容：分布式文件系统
   - 动态流：Redis Sorted Set（按时间排序）

4. **缓存策略：**
   - 用户信息缓存
   - 热点动态缓存
   - 关注关系列表缓存

#### 7.5.3 聊天系统
**问题：** 设计一个聊天系统

**参考答案：**
**需求分析：**
- 实时消息传递
- 历史消息存储
- 在线状态
- 群聊功能

**系统设计：**
1. **通信协议：**
   - WebSocket：实时通信
   - HTTP长轮询：兼容性更好

2. **消息传递：**
   ```
   客户端 → 网关 → 消息服务 → 接收方
   ```

3. **存储设计：**
   - 在线消息：内存队列
   - 离线消息：数据库
   - 历史消息：分布式存储

4. **扩展性：**
   - 网关集群
   - 消息服务集群
   - 消息队列解耦

5. **可靠性：**
   - 消息确认机制
   - 离线消息存储
   - 消息重试

### 7.6 监控和运维

#### 7.6.1 监控体系
**问题：** 如何设计系统的监控体系？

**参考答案：**
1. **基础设施监控：**
   - CPU、内存、磁盘、网络
   - 工具：Zabbix、Prometheus

2. **应用监控：**
   - 响应时间、错误率、吞吐量
   - 工具：APM（Application Performance Monitoring）

3. **业务监控：**
   - 关键业务指标
   - 自定义监控项

4. **日志监控：**
   - 错误日志
   - 访问日志
   - 工具：ELK（Elasticsearch、Logstash、Kibana）

5. **链路追踪：**
   - 分布式请求追踪
   - 工具：Zipkin、Jaeger、SkyWalking

**问题：** 如何实现分布式链路追踪？

**参考答案：**
1. **埋点：** 在服务间传递Trace ID
2. **收集：** 收集各个节点的Span信息
3. **存储：** 存储到分布式存储系统
4. **展示：** 提供可视化界面

```
Trace：一次完整的请求
├── Span：一个服务内的操作
│   ├── 开始时间
│   ├── 结束时间
│   ├── 耗时
│   └── 标签信息
```

#### 7.6.2 运维自动化
**问题：** 如何实现运维自动化？

**参考答案：**
1. **配置管理：**
   - Ansible、Puppet、Chef
   - 基础设施即代码（IaC）

2. **持续集成/持续部署（CI/CD）：**
   - Jenkins、GitLab CI、GitHub Actions
   - 自动化测试、构建、部署

3. **容器化：**
   - Docker容器化
   - Kubernetes编排

4. **自动化监控：**
   - 自动告警
   - 自动扩容
   - 自动故障恢复

5. **混沌工程：**
   - 主动注入故障
   - 验证系统韧性

### 7.7 安全设计

#### 7.7.1 认证授权
**问题：** 如何实现用户认证和授权？

**参考答案：**
1. **认证方式：**
   - Session-Cookie
   - Token（JWT）
   - OAuth 2.0
   - SSO（单点登录）

2. **授权模型：**
   - RBAC（基于角色的访问控制）
   - ABAC（基于属性的访问控制）
   - ACL（访问控制列表）

3. **安全考虑：**
   - HTTPS加密
   - 密码哈希存储
   - 防止暴力破解
   - 定期更换密钥

**问题：** JWT的原理和安全性？

**参考答案：**
**JWT结构：**
```
Header.Payload.Signature
```

**安全性：**
- 使用强加密算法（HS256、RS256）
- 设置合理的过期时间
- 不要在Payload中存储敏感信息
- 使用HTTPS传输
- 实现Token黑名单机制

#### 7.7.2 常见攻击防护
**问题：** 如何防护常见的Web攻击？

**参考答案：**
1. **SQL注入：**
   - 使用预编译语句
   - 输入验证
   - 最小权限原则

2. **XSS攻击：**
   - 输入过滤
   - 输出编码
   - CSP（内容安全策略）

3. **CSRF攻击：**
   - CSRF Token
   - SameSite Cookie
   - Referer检查

4. **DDoS攻击：**
   - 流量清洗
   - CDN防护
   - 限流
   - IP黑名单

### 7.8 容量规划

#### 7.8.1 性能评估
**问题：** 如何进行系统容量规划？

**参考答案：**
1. **性能指标：**
   - QPS（每秒查询数）
   - TPS（每秒事务数）
   - 响应时间
   - 并发用户数

2. **容量评估：**
   ```
   单机容量 × 机器数量 × 冗余系数 = 总容量
   ```

3. **压测方法：**
   - 基准测试
   - 负载测试
   - 压力测试
   - 稳定性测试

4. **扩容策略：**
   - 垂直扩容：提升单机性能
   - 水平扩容：增加机器数量

**问题：** 如何设计压测方案？

**参考答案：**
1. **确定目标：** 明确压测目的（性能瓶颈、容量评估等）
2. **准备环境：** 生产环境或准生产环境
3. **设计场景：** 模拟真实用户行为
4. **执行压测：** 逐步增加负载
5. **监控分析：** 收集各项指标
6. **优化调整：** 根据结果优化系统

#### 7.8.2 成本优化
**问题：** 如何优化系统成本？

**参考答案：**
1. **架构优化：**
   - 缓存减少数据库压力
   - 异步处理降低实时要求
   - 冷热数据分离

2. **资源优化：**
   - 合理配置服务器规格
   - 使用Spot实例
   - 自动扩缩容

3. **存储优化：**
   - 数据压缩
   - 生命周期管理
   - 选择合适的存储类型

4. **网络优化：**
   - CDN减少带宽成本
   - 数据压缩传输
   - 合理设置缓存策略
